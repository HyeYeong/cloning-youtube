extends base.pug

block head
  title #{pageTitle} | clonging youtube

block content
  div 
    p=video.description
    p=video.hashtags
    small=video.createdAt
  div
    a(href=`${video.id}/edit`) Edit video &rarr;
    br
    a(href=`${video.id}/delete`) Delete video &rarr;
