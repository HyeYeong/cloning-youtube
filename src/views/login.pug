extends base.pug

block head 
  title #{pageTitle} | clonging youtube

block content
  form(method="GET")
    span='ID'
    input(type="email", name="email", placeholder="이메일을 입력해주세요", value="", required)

    span='Password'
    input(type="password", name="password", value="", required)

    input(type="submit", value="Login")
